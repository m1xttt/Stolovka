# External host port. SmartLocker commonly uses 8000, so this project defaults to 5267.
STOLOVKA_HOST_PORT=5267

# Change this before public hosting.
FLASK_SECRET_KEY=replace-with-a-long-random-secret

STOLOVKA_WORKERS=1
STOLOVKA_THREADS=4
STOLOVKA_TIMEOUT=120
